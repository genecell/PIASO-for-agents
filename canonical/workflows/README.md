# workflows/

One file per cross-component analysis task. Each opens with its exact multi-package install
line and tracks the AnnData/Seurat state passed between steps — this tier is where the
ecosystem is documented as a whole (no single component repo covers composition).
